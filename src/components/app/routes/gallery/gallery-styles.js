export default (theme) => ({
  actionIcon: {
    paddingRight: theme.spacing.unit * 2
  },
  paper: {
    margin: '1em',
    padding: '1em'
  },
  title: {
    paddingBottom: theme.spacing.unit
  }
});
