language: node_js
node_js: 8.9.1
branches:
  only: master
cache: yarn
install: yarn
script:
- yarn build
deploy:
  fqdn: acealters.com
  github-token: $GITHUB_TOKEN
  keep-history: true
  provider: pages
  skip-cleanup: true
  on:
    branch: master
